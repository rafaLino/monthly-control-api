{
  "name": "monthly-control_lambdafunction",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll",
    "build": "cd src && zip -r monthly-control . -x '/__tests__/*'"
  },
  "author": "lino",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.637.0",
    "@aws-sdk/lib-dynamodb": "^3.637.0"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.25.4",
    "jest": "^29.7.0"
  }
}
