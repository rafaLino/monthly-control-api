{
  "name": "monthly-control-api",
  "version": "1.0.0",
  "main": "src/index.mjs",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll",
    "bundle": "cd ./src && zip -qq -r ../bundle.zip ./ -x '/__tests__/*'"
  },
  "author": "lino",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.637.0",
    "@aws-sdk/lib-dynamodb": "^3.637.0"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.25.4",
    "jest": "^29.7.0"
  }
}
